#!/bin/sh
# Sau git clone / checkout branch: tu dong restore DB neu chua co pgdata.
cd "$(git rev-parse --show-toplevel)" || exit 0
if [ ! -f "pgdata/PG_VERSION" ]; then
  echo "[LiteERP] Chua co pgdata - dang phuc hoi database tu seed..."
  cmd //c setup-database.bat
fi
